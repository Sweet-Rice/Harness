[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "harness"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.2",
    "ollama",
    "websockets",
    "duckduckgo-search>=5.0",
]

